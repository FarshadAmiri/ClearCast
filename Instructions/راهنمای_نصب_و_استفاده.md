# راهنمای نصب و راه‌اندازی ClearCast
## مستندات کامل برای توسعه‌دهندگان و کاربران

---

## 📋 فهرست مطالب

1. [پیش‌نیازها](#پیشنیازها)
2. [نصب و راه‌اندازی](#نصب-و-راهاندازی)
3. [پیکربندی](#پیکربندی)
4. [راه‌اندازی سرویس‌ها](#راهاندازی-سرویسها)
5. [استفاده از رابط کاربری](#استفاده-از-رابط-کاربری)
6. [استفاده از API](#استفاده-از-api)
7. [عیب‌یابی](#عیبیابی)
8. [بهینه‌سازی و تنظیمات پیشرفته](#بهینهسازی-و-تنظیمات-پیشرفته)

---

## 🔧 پیش‌نیازها

### نرم‌افزارهای مورد نیاز

#### حداقل:
- **Python**: نسخه 3.8 یا بالاتر
- **pip**: Package manager Python
- **Git**: برای Clone کردن repository
- **Redis**: نسخه 6.0 یا بالاتر
- **FFmpeg**: برای پردازش صوت

#### توصیه شده:
- **CUDA**: نسخه 11.7 یا بالاتر (برای GPU)
- **cuDNN**: نسخه 8.0 یا بالاتر
- **NVIDIA GPU**: با حداقل 8GB VRAM

### سیستم مورد نیاز

#### حداقل:
```
CPU: Intel i5 یا AMD Ryzen 5 (4 هسته)
RAM: 8GB
Storage: 50GB فضای خالی
GPU: اختیاری
```

#### توصیه شده:
```
CPU: Intel i7 یا AMD Ryzen 7 (8+ هسته)
RAM: 16GB+
Storage: 100GB+ SSD
GPU: NVIDIA RTX 3060 (12GB VRAM) یا بهتر
```

---

## 📥 نصب و راه‌اندازی

### گام ۱: نصب Python

#### Windows:
1. دانلود Python از [python.org](https://www.python.org/downloads/)
2. نصب با گزینه "Add Python to PATH"
3. تأیید نصب:
```cmd
python --version
pip --version
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install python3.10 python3-pip python3-venv
python3 --version
```

#### macOS:
```bash
brew install python@3.10
python3 --version
```

---

### گام ۲: نصب Redis

#### Windows:
1. دانلود Redis از [GitHub Releases](https://github.com/tporadowski/redis/releases)
2. استخراج و اجرای `redis-server.exe`

یا استفاده از Docker:
```cmd
docker run -d -p 6379:6379 redis:latest
```

#### Linux:
```bash
sudo apt install redis-server
sudo systemctl start redis
sudo systemctl enable redis

# تست
redis-cli ping
# باید PONG برگرداند
```

#### macOS:
```bash
brew install redis
brew services start redis

# تست
redis-cli ping
```

---

### گام ۳: نصب FFmpeg

#### Windows:
1. دانلود از [ffmpeg.org](https://ffmpeg.org/download.html)
2. افزودن به PATH

یا با Chocolatey:
```cmd
choco install ffmpeg
```

#### Linux:
```bash
sudo apt install ffmpeg
ffmpeg -version
```

#### macOS:
```bash
brew install ffmpeg
```

---

### گام ۴: دانلود و نصب ClearCast

#### Clone کردن Repository:
```bash
git clone https://github.com/your-repo/ClearCast.git
cd ClearCast
```

#### ایجاد Virtual Environment:

**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

#### نصب Dependencies:
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

**نکته:** نصب ممکن است ۱۰-۱۵ دقیقه طول بکشد.

---

### گام ۵: نصب CUDA (اختیاری - برای GPU)

#### Windows/Linux:
1. دانلود CUDA Toolkit 11.7+ از [developer.nvidia.com/cuda-downloads](https://developer.nvidia.com/cuda-downloads)
2. دانلود cuDNN از [developer.nvidia.com/cudnn](https://developer.nvidia.com/cudnn)
3. نصب و تنظیم PATH

#### نصب PyTorch با CUDA:
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

#### تست CUDA:
```python
import torch
print(torch.cuda.is_available())  # باید True برگرداند
print(torch.cuda.get_device_name(0))  # نام GPU شما
```

---

### گام ۶: راه‌اندازی Database

#### ایجاد و Migration:
```bash
python manage.py makemigrations
python manage.py migrate
```

#### ایجاد SuperUser:
```bash
python manage.py createsuperuser
```

مشخصات خواسته شده:
- Username: admin (یا هر نام دلخواه)
- Email: admin@example.com
- Password: ********

---

### گام ۷: دانلود مدل‌های AI

#### HuggingFace Token (برای دسترسی به مدل‌ها):

1. ثبت‌نام در [huggingface.co](https://huggingface.co/)
2. دریافت Access Token از Settings > Access Tokens
3. ذخیره Token:

```bash
# ایجاد فایل hf_token.txt در root پروژه
echo "your_token_here" > hf_token.txt
```

#### دانلود خودکار مدل‌ها:

مدل‌ها در اولین استفاده به صورت خودکار دانلود می‌شوند:

```python
# اولین بار که سرویس استفاده می‌شود، مدل‌ها دانلود می‌شوند
# محل ذخیره: ~/.cache/huggingface/
```

---

## ⚙️ پیکربندی

### فایل تنظیمات: `.env`

در root پروژه، فایل `.env` ایجاد کنید:

```bash
# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite by default)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Audio Processing
AUDIO_CHUNK_SECONDS=2.0
AUDIO_OVERLAP_SECONDS=0.5
BROWSER_AUDIO_PROCESSING=True

# File Upload
MAX_UPLOAD_SIZE=209715200  # 200MB in bytes

# CUDA/GPU
USE_GPU=True
CUDA_DEVICE=0

# HuggingFace
HF_TOKEN=your_huggingface_token
HF_HOME=/path/to/cache
```

### پیکربندی Redis (در صورت نیاز):

فایل `redis.conf`:
```conf
bind 127.0.0.1
port 6379
maxmemory 1gb
maxmemory-policy allkeys-lru
```

---

## 🚀 راه‌اندازی سرویس‌ها

### روش ۱: راه‌اندازی دستی (Development)

#### Terminal 1: Redis
```bash
redis-server
```

#### Terminal 2: Django Development Server
```bash
python manage.py runserver 0.0.0.0:8000
```

#### Terminal 3: Daphne (برای WebSocket)
```bash
daphne -b 0.0.0.0 -p 8001 audio_stream_project.asgi:application
```

### روش ۲: استفاده از Script (Windows)

فایل `run_dev.bat`:
```batch
@echo off
start cmd /k redis-server
timeout /t 2
start cmd /k python manage.py runserver 0.0.0.0:8000
start cmd /k daphne -b 0.0.0.0 -p 8001 audio_stream_project.asgi:application
echo Servers started!
```

### روش ۳: استفاده از Docker Compose (Production)

فایل `docker-compose.yml`:
```yaml
version: '3.8'

services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
  
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - redis
  
  websocket:
    build: .
    command: daphne -b 0.0.0.0 -p 8001 audio_stream_project.asgi:application
    volumes:
      - .:/app
    ports:
      - "8001:8001"
    depends_on:
      - redis
```

اجرا:
```bash
docker-compose up -d
```

---

## 🖥 استفاده از رابط کاربری

### دسترسی به سیستم

1. باز کردن مرورگر
2. مراجعه به: `http://localhost:8000`
3. ورود با اطلاعات SuperUser

### صفحه اصلی (Dashboard)

```
┌─────────────────────────────────────────────────────┐
│  ClearCast                        [Profile] [Logout] │
├─────────────────────────────────────────────────────┤
│                                                      │
│  🎵 Denoise a File                                  │
│  🎤 Vocal Separation                                │
│  📢 Audio Boost                                      │
│  👤 Speaker Extraction                              │
│  🎙️ Speaker Diarization                            │
│  📡 Live Streaming                                   │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

### سرویس ۱: حذف نویز فایل

#### مراحل:
1. کلیک روی "Denoise a File"
2. انتخاب فایل صوتی
3. انتخاب سطح تقویت (اختیاری)
4. کلیک "Upload"
5. منتظر پردازش بمانید
6. دانلود فایل تمیز شده

![Denoise Modal](path/to/screenshot)

**فرمت‌های پشتیبانی شده:**
- MP3
- WAV
- M4A
- FLAC

**حداکثر اندازه:** 200MB

---

### سرویس ۲: جداسازی آواز

#### مراحل:
1. کلیک روی "Vocal Separation"
2. آپلود فایل موزیک/آهنگ
3. انتخاب نوع خروجی:
   - Vocals (فقط آواز)
   - Instrumental (فقط موسیقی)
4. شروع پردازش
5. دانلود نتیجه

**زمان پردازش:** حدود 1-2 دقیقه برای یک آهنگ 3-4 دقیقه‌ای

---

### سرویس ۳: تقویت صدا

#### مراحل:
1. کلیک "Audio Boost"
2. آپلود فایل با Volume پایین
3. انتخاب سطح تقویت (2x تا 5x)
4. پردازش
5. دانلود فایل تقویت شده

---

### سرویس ۴: استخراج گوینده

#### مراحل:
1. کلیک "Speaker Extraction"
2. آپلود فایل اصلی (چند نفره)
3. آپلود نمونه صوتی گوینده مورد نظر
4. انتخاب تقویت (اختیاری)
5. شروع پردازش
6. دانلود صدای استخراج شده

**نکات:**
- نمونه صوتی باید حداقل 3 ثانیه باشد
- کیفیت بهتر نمونه = نتیجه بهتر
- فایل اصلی می‌تواند تا 1 ساعت باشد

---

### سرویس ۵: استریم زنده

#### برای Broadcaster (پخش‌کننده):

1. کلیک "Start Streaming"
2. دادن اجازه دسترسی به میکروفون
3. انتخاب گزینه‌ها:
   - Enable Noise Reduction: حذف نویز زنده
   - Enable Recording: ضبط استریم
4. شروع پخش
5. لینک استریم به دوستان

#### برای Listener (شنونده):

1. کلیک روی نام دوست در لیست Online Friends
2. پخش خودکار صدا
3. امکان Chat همزمان

---

### سرویس ۶: دایرایزیشن (Speaker Diarization)

این سرویس از طریق Jupyter Notebook یا Python Script استفاده می‌شود.

#### استفاده از Notebook:

1. باز کردن `Nemo-diarization/test_diarization.ipynb`
2. اجرای Cell های Setup
3. تنظیم مسیرهای فایل
4. اجرای Cell های پردازش
5. مشاهده نتایج

**مراحل دقیق در فایل Notebook:**
```python
# 1. Import modules
from main import process_meeting_audio

# 2. Create speaker database
create_voice_database(speaker_samples, "speakers_db.json")

# 3. Process meeting
result = process_meeting_audio(
    "meeting.mp3",
    "speakers_db.json",
    expected_language="fa",
    output_transcriptions=True
)

# 4. View results
for seg in result['segments']:
    print(f"{seg['speaker']}: {seg['text']}")
```

---

## 🔍 عیب‌یابی

### مشکل ۱: Redis Connection Error

**علامت:**
```
redis.exceptions.ConnectionError: Error connecting to Redis
```

**راه‌حل:**
1. اطمینان از اجرای Redis:
```bash
redis-cli ping
```

2. بررسی پورت:
```bash
netstat -an | grep 6379
```

3. ری‌استارت Redis:
```bash
# Linux
sudo systemctl restart redis

# Windows
redis-server.exe
```

---

### مشکل ۲: CUDA Out of Memory

**علامت:**
```
RuntimeError: CUDA out of memory
```

**راه‌حل:**
1. کاهش Batch Size در settings
2. استفاده از مدل کوچکتر
3. استفاده از CPU:
```python
# در فایل .env
USE_GPU=False
```

---

### مشکل ۳: مدل دانلود نمی‌شود

**علامت:**
```
HTTPError: 401 Client Error: Unauthorized
```

**راه‌حل:**
1. بررسی HuggingFace Token در `hf_token.txt`
2. لاگین در HuggingFace:
```bash
pip install huggingface-hub[cli]
huggingface-cli login
```

---

### مشکل ۴: WebRTC Connection Failed

**علامت:**
اتصال برقرار نمی‌شود یا صدا قطع می‌شود

**راه‌حل:**
1. بررسی HTTPS (WebRTC نیاز به HTTPS دارد)
2. بررسی Firewall
3. استفاده از TURN Server در Production

---

### مشکل ۵: فایل آپلود نمی‌شود

**علامت:**
```
File size exceeds maximum allowed
```

**راه‌حل:**
افزایش حد مجاز در `settings.py`:
```python
DATA_UPLOAD_MAX_MEMORY_SIZE = 209715200  # 200MB
FILE_UPLOAD_MAX_MEMORY_SIZE = 209715200
```

---

## ⚡ بهینه‌سازی و تنظیمات پیشرفته

### بهینه‌سازی GPU

#### تنظیم Memory Growth:
```python
# در audio_processor.py
import tensorflow as tf

gpus = tf.config.experimental.list_physical_devices('GPU')
for gpu in gpus:
    tf.config.experimental.set_memory_growth(gpu, True)
```

#### استفاده از Mixed Precision:
```python
from torch.cuda.amp import autocast

with autocast():
    output = model(input_audio)
```

---

### بهینه‌سازی Redis

#### تنظیمات Optimal:
```conf
# redis.conf
maxmemory 2gb
maxmemory-policy allkeys-lru
save 900 1
save 300 10
```

---

### بهینه‌سازی Django

#### استفاده از Gunicorn (Production):
```bash
pip install gunicorn
gunicorn audio_stream_project.wsgi:application \
    --bind 0.0.0.0:8000 \
    --workers 4 \
    --threads 2
```

#### استفاده از Nginx (Reverse Proxy):
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location /static/ {
        alias /path/to/staticfiles/;
    }

    location /media/ {
        alias /path/to/media/;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /ws/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

---

### Logging و Monitoring

#### تنظیم Logging:
```python
# settings.py
LOGGING = {
    'version': 1,
    'handlers': {
        'file': {
            'level': 'INFO',
            'class': 'logging.FileHandler',
            'filename': 'clearcast.log',
        },
    },
    'loggers': {
        'django': {
            'handlers': ['file'],
            'level': 'INFO',
        },
    },
}
```

---

### Backup و Restore

#### Backup Database:
```bash
python manage.py dumpdata > backup.json
```

#### Restore:
```bash
python manage.py loaddata backup.json
```

#### Backup Media Files:
```bash
tar -czf media_backup.tar.gz media/
```

---

## 📞 پشتیبانی

در صورت بروز مشکل:
1. بررسی Logs: `clearcast.log`
2. بررسی GitHub Issues
3. ایمیل به support@clearcast.example
4. مراجعه به مستندات: `Instructions/`

---

**© 2024-2026 ClearCast Team**
